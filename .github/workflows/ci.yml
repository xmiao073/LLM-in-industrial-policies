name: CI (noop)

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: No-op (temporary)
        run: |
          echo "CI temporarily disabled: noop job"
          echo "Revert this change to run real tests again"

